Confluence Calculator — Mobile PWA (v3 FULL)
====================================================
- Readable labels (wrap, no overlap)
- Edit Mode to rename items on-device (saved)
- W/D/4H sync 10% each, text size control
- Multi-market workspaces, Save Image (PNG), Export JSON
- Offline-capable PWA

Deploy: upload all files to repo root, enable GitHub Pages (root), refresh phone or reinstall PWA if cached.
